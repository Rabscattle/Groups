storage:
  # Options are MYSQL & MARIADB
  type: MARIADB
  url: db
  port: 3306
  # Your database must be created before you run this plugin
  db: test
  user: test
  password: BNMCSBH2432IUHOI32GJ

# This will reformat your chat messages
# Placeholders you can use:
# => {player_group_prefix}
# => {player_group_name}
# ...
chat:
  enabled: true
  format: "{player_group_prefix} %s > %s"

# This will format signs
# containing "group-sign"
group-signs:
  enabled: true
  # Radius where the sign updates to players
  update-radius: 25
  # 20 Ticks = 1s. Speed at which updates of signs occur
  update-tick-speed: 60
